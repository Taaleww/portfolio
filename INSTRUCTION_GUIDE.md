# คู่มือการแก้ไขข้อความและสไตล์ (สำหรับผู้เริ่มต้น/เขียนโค้ดไม่เป็น) 🎨✍️

ยินดีต้อนรับสู่คู่มือแนะนำวิธีแก้ไขข้อความ รูปภาพ และปรับแต่งหน้าตาร้านค้า/Portfolio ของคุณด้วยตัวเองอย่างง่าย ๆ โดยไม่จำเป็นต้องเขียนโค้ดเป็นทั้งหมด!

---

## 📂 1. โครงสร้างไฟล์ในโปรเจกต์นี้ (อยากแก้ส่วนไหน ไปที่ไฟล์ไหน?)

โค้ดทั้งหมดที่แสดงผลบนหน้าเว็บจะอยู่ในโฟลเดอร์ `src/components` และ `src/pages` เป็นหลัก นี่คือรายชื่อไฟล์ที่คุณสามารถเปิดเพื่อแก้ไขได้ทันที:

| ส่วนของหน้าเว็บ | ลิงก์ไฟล์ที่ต้องเปิดเพื่อแก้ไข | ประเภทไฟล์ | คำอธิบาย |
| :--- | :--- | :--- | :--- |
| **เมนูบาร์ด้านบน** | [Header.astro](file:///Users/taaleww/Desktop/portfolio/portfolio/src/components/Header.astro) | Astro | เมนูนำทางด้านบน (About, Work, Contact, Resume) |
| **ส่วนแรกสุด (หน้าต้อนรับ)** | [Hero.astro](file:///Users/taaleww/Desktop/portfolio/portfolio/src/components/Hero.astro) | Astro | ชื่อขนาดใหญ่ยินดีต้อนรับ, คำอธิบายสั้น ๆ และภาพพื้นหลังแรก |
| **ประวัติ / แนะนำตัว** | [Introduction.astro](file:///Users/taaleww/Desktop/portfolio/portfolio/src/components/Introduction.astro) | Astro | รายละเอียดของคุณ ประสบการณ์ และประวัติการทำงาน |
| **แกลเลอรีผลงาน** | [ProjectGallery.tsx](file:///Users/taaleww/Desktop/portfolio/portfolio/src/components/ProjectGallery.tsx) | React (TSX) | กล่องแสดงผลงาน/โปรเจกต์ต่าง ๆ |
| **ขั้นตอนการทำงาน** | [Workflow.astro](file:///Users/taaleww/Desktop/portfolio/portfolio/src/components/Workflow.astro) | Astro | ลำดับขั้นตอนการทำงานของคุณ |
| **ส่วนติดต่อด้านล่าง** | [Contact.astro](file:///Users/taaleww/Desktop/portfolio/portfolio/src/components/Contact.astro) | Astro | ฟอร์มสำหรับติดต่อ, อีเมล และลิงก์โซเชียลมีเดีย |
| **ท้ายสุดของหน้าเว็บ** | [Footer.astro](file:///Users/taaleww/Desktop/portfolio/portfolio/src/components/Footer.astro) | Astro | ลิขสิทธิ์ (Copyright) และรายละเอียดท้ายสุด |

---

## ✍️ 2. วิธีการแก้ไขข้อความ (Editing Text)

หากคุณต้องการเปลี่ยนคำพูด หรือแก้ไขตัวสะกด:

1. **เปิดไฟล์** ตามตารางด้านบนในโปรแกรมแก้ไขโค้ด (เช่น VS Code)
2. **กดค้นหา (Ctrl + F หรือ Cmd + F)** แล้วพิมพ์ข้อความเดิมที่คุณต้องการจะเปลี่ยน
3. **แก้ไขเฉพาะข้อความ** ที่อยู่ระหว่างสัญลักษณ์ Tag ของ HTML ตัวอย่างเช่น:
   - ❌ **ห้ามลบสัญลักษณ์ปีกกา หรือ Tag แหลม ๆ** (เช่น `<p>`, `</p>`, `<div>`, `class="..."`)
   - **ตัวอย่างการแก้ไขที่ถูกต้อง:**
     ```html
     <!-- ก่อนแก้ -->
     <p class="text-black">Meet your trusted UX/UI designer...</p>

     <!-- หลังแก้ (เปลี่ยนเฉพาะข้อความด้านใน) -->
     <p class="text-black">สวัสดีครับ! ยินดีต้อนรับสู่ Portfolio ของผม...</p>
     ```

### ⚠️ ข้อควรระวังระหว่างแก้ไข:
- ในไฟล์ **`ProjectGallery.tsx`** ซึ่งเป็น React component หากมีข้อความอยู่ภายใต้สัญลักษณ์ปีกกา `{ }` หรือเครื่องหมายคำพูดคู่ `"..."` ให้ระวังการเผลอไปลบเครื่องหมายเหล่านี้ เพราะอาจจะทำให้โปรแกรมทำงานผิดพลาดได้

---

## 🎨 3. วิธีปรับแต่งหน้าตาและสีสัน (Styling & CSS)

โปรเจกต์นี้ใช้ระบบที่ชื่อว่า **Tailwind CSS** ในการจัดแต่งหน้าตา ซึ่งจุดเด่นคือสไตล์ทั้งหมดจะเขียนรวมอยู่ใน attribute ที่ชื่อว่า `class="..."` (หรือ `className="..."` ในไฟล์ `.tsx`)

คุณสามารถปรับแต่งเบื้องต้นได้ง่าย ๆ โดยการแก้ไข "รหัสสไตล์" เหล่านี้:

### 🅰️ การปรับแต่งตัวอักษร (Typography)
*   **ขนาดอักษร:** ค้นหาคำที่ขึ้นต้นด้วย `text-`
    *   `text-[16px]` หรือ `text-[24px]` (แก้ไขตัวเลขในวงเล็บเหลี่ยมเพื่อปรับขนาดฟอนต์ตามต้องการ)
*   **ความหนาตัวอักษร:** ค้นหาคำที่เขียนว่า `font-`
    *   `font-[400]` (ปกติ) ➡️ เปลี่ยนเป็น `font-[700]` หรือ `font-bold` (ตัวหนา)
*   **สีของตัวอักษร:**
    *   `text-black` (ตัวหนังสือสีดำ) ➡️ `text-gray-500` (ตัวหนังสือสีเทา)
    *   `text-white` (ตัวหนังสือสีขาว)

### 📐 การปรับระยะห่าง (Spacing)
*   **ระยะห่างรอบนอก (Margin):** ค้นหาคำที่ขึ้นต้นด้วย `m`
    *   `mb-[32px]` (Margin Bottom - ระยะห่างด้านล่าง 32 พิกเซล) ➡️ เปลี่ยนตัวเลขเพื่อขยับพื้นที่ว่างให้มากขึ้นหรือน้อยลง เช่น `mb-[50px]`
*   **ระยะห่างขอบใน (Padding):** ค้นหาคำที่ขึ้นต้นด้วย `p`
    *   `p-6` หรือ `px-[20px]` (Padding left & right - ขยับข้อความให้อยู่ห่างจากขอบซ้ายขวา 20 พิกเซล)

### 🎨 สไตล์แบบกำหนดเอง (Custom/Inline CSS)
ในบางจุด จะมีการกำหนดสไตล์โดยใช้ `style="..."` เช่น:
```html
<h1 class="..." style="color: rgba(42, 37, 37, 0.9); letter-spacing: 1%;">
```
- คุณสามารถแก้ไขสีตรง **`color: rgba(42, 37, 37, 0.9);`** ให้เป็นชื่อสีทั่วไป เช่น `color: red;` หรือค่าสี Hex Code เช่น `color: #333333;` ได้เช่นกัน

---

## 🚀 4. วิธีดูผลลัพธ์ขณะแก้ไข (How to Test)

เพื่อให้คุณเห็นผลลัพธ์ของการแก้ไขทันทีแบบ Real-time:

1. เปิด Terminal (หน้าต่างคำสั่ง) ในโปรแกรมของคุณ
2. รันคำสั่งนี้เพื่อเปิดโหมดพัฒนา:
   ```bash
   pnpm dev
   ```
3. เปิดบราวเซอร์ไปที่ลิงก์นี้: **[http://localhost:4321/portfolio](http://localhost:4321/portfolio)**
4. เมื่อคุณแก้ไขและกดบันทึกไฟล์ (Save) หน้าเว็บในบราวเซอร์จะอัปเดตคำและสีใหม่ให้โดยอัตโนมัติ!

---

💡 **คำแนะนำ:** หากบันทึกไฟล์ไปแล้วหน้าจอเปลี่ยนไปจนดูพัง หรือขึ้นข้อความ Error สีแดง ไม่ต้องตกใจครับ! เพียงกดเลิกทำ (`Ctrl + Z` หรือ `Cmd + Z`) เพื่อย้อนกลับไปจุดเดิมก่อนหน้าแล้วกดบันทึกใหม่อีกครั้ง ทุกอย่างก็จะกลับมาใช้งานได้ตามปกติครับ
